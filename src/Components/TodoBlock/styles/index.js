import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 0.5,
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 5,
  },
});

export default styles;
